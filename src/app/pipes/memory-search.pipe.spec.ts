import { MemorySearchPipe } from './memory-search.pipe';

describe('MemorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MemorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
